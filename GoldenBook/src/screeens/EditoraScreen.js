import * as React from 'react';
import { View, Text } from 'react-native';
import { Searchbar } from 'react-native-paper';

function EditoraScreen() {
    return (
        <View style={{backgroundColor: '#f5f5f5', flex: 1}}>
            <Text>ola</Text>
        </View>
    );
}

export default EditoraScreen;